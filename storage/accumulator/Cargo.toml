[package]
name = "creditchain-accumulator"
description = "CreditChain merkle tree accumulator"
version = "0.1.1"

# Workspace inherited keys
authors = { workspace = true }
edition = { workspace = true }
homepage = { workspace = true }
license = { workspace = true }
publish = { workspace = true }
repository = { workspace = true }
rust-version = { workspace = true }

[dependencies]
anyhow = { workspace = true }
creditchain-crypto = { workspace = true }
creditchain-types = { workspace = true }
proptest = { workspace = true, optional = true }

[dev-dependencies]
creditchain-crypto = { workspace = true, features = ["fuzzing"] }
proptest = { workspace = true }
rand = { workspace = true }

[features]
default = []
fuzzing = ["proptest", "creditchain-crypto/fuzzing", "creditchain-types/fuzzing"]
