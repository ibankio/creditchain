name: Copy images to dockerhub on release
on:
  push:
    branches:
      # creditchain-node network-specific release branches
      - devnet
      - testnet
      - mainnet
      # preview branches
      - preview
      - preview-networking
      # creditchain-indexer-grpc network-specific release branches
      - creditchain-indexer-grpc-devnet
      - creditchain-indexer-grpc-testnet
      - creditchain-indexer-grpc-mainnet
    tags:
      - creditchain-node-v*
      - creditchain-indexer-grpc-v*

permissions:
  contents: read
  id-token: write #required for GCP Workload Identity federation

jobs:
  copy-images-to-docker-hub:
    uses: ./.github/workflows/copy-images-to-dockerhub.yaml
    with:
      image_tag_prefix: ${{ github.ref_name }}
    secrets: inherit
