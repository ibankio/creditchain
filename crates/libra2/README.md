# CreditChain Command Line Interface (CLI) Tool

The `libra2` tool is a command line interface (CLI) for debugging, development, and node operation.

See [CreditChain CLI Documentation](https://github.com/ibankio/creditchain/tree/main/docs) for how to install the `libra2` CLI tool and how to use it.
